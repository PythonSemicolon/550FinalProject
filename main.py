# For every translator

    # For every work of the translator

        # Load work

        # Split work in fragments of length win_length by bootstrapping

        # Perform stylometric analysis on each fragment with winSize = win_length

        # Save in X_train_translator, y_train_translator

    # Train linear classifier

    # Load test work

    # Split work in fragments of length win_length by bootstrapping

    # Perform stylometric analysis on each fragment with winSize = win_length

    # Save in X_test_translator, y_test_translator

    # Predict

    # Save results in results_translator

